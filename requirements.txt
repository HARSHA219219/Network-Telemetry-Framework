# Phase 1: only what's needed to import/lint the current skeleton.
# Phase 2: adds pysnmp for real SNMPv2c polling.
# Phase 3: pure-math metric engine, no new dependency.
# Phase 4-5: ICMP latency + simulator, no new dependency (stdlib subprocess only).
# Phase 6-7: protobuf/gRPC transport.
# Phase 8-9: PostgreSQL persistence + FastAPI REST backend.
# OpenTelemetry packages are intentionally optional (see backend/telemetry/otel_setup.py)
# and are NOT listed here - the backend runs correctly without them.

PyYAML==6.0.2
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0
pysnmp==6.2.6

grpcio==1.68.1
grpcio-tools==1.68.1

SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.1
